{
  "name": "breaking_bad_backend",
  "version": "1.0.0",
  "description": "backend for my react app",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "app": "nodemon app.js",
    "client": "npm start --prefix client",
    "get-client": "cd client && npm install",
    "dev": "concurrently \"npm run app\" \"npm run client \""
  },
  "author": "Om1621",
  "license": "MIT",
  "dependencies": {
    "concurrently": "^5.3.0",
    "cors": "^2.8.5",
    "express": "^4.17.1"
  }
}
