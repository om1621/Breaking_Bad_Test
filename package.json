{
  "name": "breaking_bad_backend",
  "version": "1.0.0",
  "description": "backend for my react app",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node app.js",
    "heroku-postbuild":"cd client && npm install && npm install --only=dev --no-shrinkwrap && npm run buiild"
  },
  "author": "Om1621",
  "license": "MIT",
  "dependencies": {
    "concurrently": "^5.3.0",
    "cors": "^2.8.5",
    "express": "^4.17.1"
  }
}
